import React from 'react';
import Connect from '../components/Connect';
import Header from '../components/Header';

const Home = () => {
    return (
        <div>
            <Header />
            <Connect />
        </div>
    );
};

export default Home;